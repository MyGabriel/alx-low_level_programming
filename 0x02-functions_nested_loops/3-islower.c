/* Description: A pro to check abc */
#include "main.h"

/**
 * _islower - prints 1 or 0 depending on input
 * @c: first parameter
 *
 * Return: Always(0).
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
