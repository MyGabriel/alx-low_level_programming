/* File: A func that add 2 int and ret rest */
#include "main.h"

/**
 * add - add two numbers from input
 * @s: first parameter
 * @t: second parameter
 * Return: Always (0).
 */

int add(int s, int t)
{
	return (s + t);
}
