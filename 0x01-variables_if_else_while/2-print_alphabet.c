/* Description: Printing alphabets A-Z */
#include <time.h>
#include <stdio.h>
#include <stdlib.h>

/**
* main - set to void
* Return: 0
* Using only putchar but not more than 2
*/

int main(void)
{
	char _alphabet = 'a';
	
	_alphabet == _alphabet++;
		{
			putchar(_alphabet);

			while
				(_alphabet <= 'z');
		}
	putchar('\n');
	return (0);
}
